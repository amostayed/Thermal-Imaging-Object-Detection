1. Bounding box clustering (for thermal training images) to find anchors (code in Jupyter notebook)
   Just follow the notebook!
2. I am using 5 anchors per scale for my experimentations
3. One could experiment with 3 anchors (YOLOv3/v4, etc.) or anchor-less (YOLOX) models